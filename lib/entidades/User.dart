class User {
  final int id;
  final String email;
  final String name;
  final String surname;
  final DateTime created_at;

  const User(this.id, this.email, this.name, this.surname, this.created_at);
}
